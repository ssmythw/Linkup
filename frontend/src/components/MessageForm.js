import React from "react";
import "../styles/messageForm.css";

const MessageForm = () => {
  const handleSubmit = (e) => {
    e.preventDefault();
  };

  return (
    <>
      <div>chat</div>
    </>
  );
};

export default MessageForm;
